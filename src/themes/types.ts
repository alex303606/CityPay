export type ITheme = {
  backgroundColor: string;
  tabBarColor: string;
  tabActiveColor: string;
  tabInactiveColor: string;
  buttonColor: string;
  textColor: string;
};

export enum Themes {
  DEFAULT = 'default',
  RED = 'red',
  BLUE = 'blue',
}
